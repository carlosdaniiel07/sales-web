@model  IEnumerable<SalesWeb.Models.Venda>

@{
    ViewData["Title"] = "Relatório de vendas por período";
}

<h2>@ViewData["Title"]</h2>

<div class="panel panel-primary">
    <div class="panel-heading">Parâmetros</div>
    <div class="panel-body">
        <div class="row">
            <div class="col-md-3">
                <form>
                    <div class="form-group">
                        <label for="dataMinima" class="control-label">Data de início</label>
                        <input id="dataMinima" name="dataMinima" type="date" class="form-control" value="@ViewData["dataMinima"]" required/>
                    </div>
                    <div class="form-group">
                        <label for="dataLimite" class="control-label">Data limite</label>
                        <input id="dataLimite" name="dataLimite" type="date" class="form-control" value="@ViewData["dataLimite"]" required/>
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary ">Buscar&nbsp; <span class="glyphicon glyphicon-zoom-in" aria-hidden="true"></span></button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <table class="table table-striped">
        <thead>
            <tr class="bg-primary">
                <th>@Html.DisplayNameFor(model => model.Data)</th>
                <th>@Html.DisplayNameFor(model => model.Vendedor.Nome)</th>
                <th>@Html.DisplayNameFor(model => model.Vendedor.Departamento.Nome)</th>
                <th>@Html.DisplayNameFor(model => model.Valor)</th>
                <th>@Html.DisplayNameFor(model => model.Status)</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Data)</td>
                    <td>@Html.DisplayFor(modelItem => item.Vendedor.Nome)</td>
                    <td>@Html.DisplayFor(modelItem => item.Vendedor.Departamento.Nome)</td>
                    <td>@Html.DisplayFor(modelItem => item.Valor)</td>
                    <td>@Html.DisplayFor(modelItem => item.Status)</td>
                </tr>
            }
        </tbody>
    </table>

</div>